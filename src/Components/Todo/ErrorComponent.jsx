import React from "react";

// error component for invalid URL
function ErrorComponent() {
    return (
        <div>
            Error has occured! Contact support @123456789
        </div>
    );
};

export default ErrorComponent;